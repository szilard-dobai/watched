---
description: Auto-fix linting and formatting issues
---

Automatically fix code quality issues:

1. Run ESLint with auto-fix
2. Run Prettier to format code

Execute: `npm run lint:fix && npm run format`

Report what was fixed and if any manual fixes are still needed.
